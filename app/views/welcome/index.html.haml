%h1 Developer Productivity
%p this is the homepage that will show a list of locations that a developer can work at
%p it will also show some github analytics

- if current_user
  %a{:href => "/github", :title => "Test github_call"} Test

%br

%button Add Location
/ #location_form
/ = form_tag '/locations' do
/ = field_set_tag do
/   = label_tag :name, 'Name:'
/   = text_field_tag :name
/   = label_tag :address, 'Address:'
/   = text_field_tag :address
/   / add more fields for the rest
/   = submit_tag 'Add Location'
/ create a form for a new location

/ Make the form hidden, and toggle it when "Add Locaion" button is clicked
= form_for '/locations' do |f|
  %p
    = f.label :name
    %br
    = f.text_field :name
  %p
    = f.label :address
    %br
    = f.text_field  :address
  %p
    = f.label :city
    %br
    = f.text_field :city
  %p.button
    = f.submit 'Create Location'

#locations
#detail

= render partial: 'location'
= render partial: 'detail'
